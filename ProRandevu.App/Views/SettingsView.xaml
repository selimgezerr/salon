<UserControl x:Class="ProRandevu.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="10">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <Label Content="Tema" Width="120" />
            <ComboBox Width="120" SelectedItem="{Binding Theme}">
                <ComboBoxItem Content="Light" />
                <ComboBoxItem Content="Dark" />
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <Label Content="Yedekleme Yolu" Width="120" />
            <TextBox Text="{Binding BackupPath, UpdateSourceTrigger=PropertyChanged}" Width="200" />
            <Button Content="..." Command="{Binding BrowseCommand}" Margin="5,0,0,0" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <Label Content="Varsayılan Süre (dk)" Width="120" />
            <TextBox Width="60" Text="{Binding DefaultDuration, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <Label Content="Lisans Anahtarı" Width="120" />
            <TextBox Width="200" Text="{Binding LicenseKey, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <Button Content="Kaydet" Command="{Binding SaveCommand}" Width="100" />
    </StackPanel>
</UserControl>
